---
import { jokes } from '../data/content';
---

<div class="space-y-0">
  {jokes.map((joke, i) => (
    <>
      {i > 0 && <hr class="hairline my-3" />}
      <p class="text-ink text-sm leading-relaxed italic">{joke}</p>
    </>
  ))}
</div>

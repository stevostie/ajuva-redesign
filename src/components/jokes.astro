---
import { jokes } from '../data/content';
---

<div class="space-y-0 text-sm text-ink leading-relaxed">
  {jokes.map((joke, i) => (
    <>
      {i > 0 && <hr class="hairline my-3" />}
      <p class="italic">{joke}</p>
    </>
  ))}
</div>

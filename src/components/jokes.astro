---
import { jokes } from '../data/content';
---

<div class="space-y-0">
  {jokes.map((joke, i) => (
    <>
      {i > 0 && <hr class="border-line my-4" />}
      <p class="text-ink-700 leading-relaxed italic text-[15px]">{joke}</p>
    </>
  ))}
</div>
